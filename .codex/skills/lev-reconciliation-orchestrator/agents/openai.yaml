interface:
  display_name: "LEV Reconciliation Orchestrator"
  short_description: "Coordinate agent-led LEV reconciliation."
  default_prompt: "Use $lev-reconciliation-orchestrator to reconcile LEV public updates, produce agent recommendations, ask for approval on escalations, and execute approved editorial workflows."
